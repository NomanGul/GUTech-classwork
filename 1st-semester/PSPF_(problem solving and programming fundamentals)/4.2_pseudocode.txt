START
  MISTAKE_COUNT = 0
  MACHINE_NUMBER = 4
  INPUT guessed_number
  IF guessed_number = MACHINE_NUMBER THEN
    OUTPUT MISTAKE_COUNT
  ENDIF
  WHILE guessed_number != MACHINE_NUMBER DO
    MISTAKE_COUNT = MISTAKE_COUNT + 1
    INPUT guessed_number
  ENDWHILE
  OUTPUT MISTAKE_COUNT
END
